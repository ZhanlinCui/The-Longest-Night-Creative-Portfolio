# Documentation

| File | Purpose |
|------|---------|
| `The_Longest_Night_design_doc.md` | Information architecture, scene copy, interaction spec, visual direction |
| `The_Longest_Night_Prompt_Production_Document.md` | Production / prompt notes for the project |
| `design-system-speculative-archive.md` | “Speculative archive” UI system (surfaces, typography, do / don’t) |

**Frontend (implemented app):** `../web/` — run `npm install` and `npm run dev` inside that folder.

**Stitch HTML + PNG references:** `../reference/stitch/` — Google Stitch exports (`code.html`, `screen.png` per screen).
